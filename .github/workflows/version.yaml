name: GitVersion Usage

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  GitVersion:
    runs-on: ubuntu:24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install GitVersion
      uses: gittools/actions/gitversion/setup@v3.1.1
      with:
        versionSpec: '6.0.x'
        includePrerelease: true

    - name: Determine Version
      uses: gittools/actions/gitversion/execute@v3.1.1

    - name: Determine Version with Config File
      uses: gittools/actions/gitversion/execute@v3.1.1
      with:
        useConfigFile: true
    
    - name: Determine Version with Disabled Cache and Normalization
      uses: gittools/actions/gitversion/execute@v3.1.1
      with:
        disableCache: true
        disableNormalization: true